default_platform(:ios)

platform :ios do
  lane :beta do
    if ENV["CI"]
      create_keychain(
        name: "ci.keychain",
        password: "ci_password",
        default_keychain: true,
        unlock: true,
        timeout: 3600,
        add_to_search_list: true
      )
      ENV["MATCH_KEYCHAIN_NAME"] = "ci.keychain"
      ENV["MATCH_KEYCHAIN_PASSWORD"] = "ci_password"
    end

    api_key = app_store_connect_api_key(
      key_id: ENV['ASC_KEY_ID'],
      issuer_id: ENV['ASC_ISSUER_ID'],
      key_content: ENV['ASC_API_KEY']
    )

    match(
      type: "appstore", # ✅ 올바른 type (appstore)
      readonly: false,
      git_url: ENV['MATCH_REPO_URL'],
      api_key: api_key,
      force: true
    )

    increment_build_number(
      xcodeproj: "SOAPFT.xcodeproj"
    )

    build_app(
      scheme: "SOAPFT",
      configuration: "Release", # ✅ TestFlight 배포용
      export_method: "app-store", # ✅ 정확한 export_method
      export_options: {
        provisioningProfiles: {
          "io.tuist.SOAPFT" => "match AppStore io.tuist.SOAPFT 1750697335"
        }
      }
    )

    deliver(
      api_key: api_key,
      submit_for_review: false,
      skip_metadata: true,
      skip_screenshots: true
    )
  end
end
